apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
data:
  # base64-encoded values from your .env
  postgres-user: dXNlcg==            # 'user'
  postgres-password: cGFzc3dvcmQ=    # 'password'
  postgres-db: cmVsYXlkYg==          # 'relaydb'
