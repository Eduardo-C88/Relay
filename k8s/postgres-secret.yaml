apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
data:
  # base64-encoded values from your .env
  DATABASE_USER: dXNlcg==            # 'user'
  DATABASE_PASSWORD: cGFzc3dvcmQ=    # 'password'
  DATABASE_DB: cmVsYXlkYg==          # 'relaydb'
